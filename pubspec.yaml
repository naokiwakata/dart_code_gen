name: dart_code_gen
description: A sample command-line application.
version: 1.0.0

environment:
  sdk: ^3.0.7

dependencies:
  build: ^2.4.1

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
  build_runner: ^2.4.6
  source_gen: ^1.4.0
